pragma solidity 0.4.24;

import "../../contracts/{{ name.Plural }}.sol";


contract {{ name.Plural }}FunctionParametersScenario {
    using {{ name.Plural }} for {{ name.Plural }}.{{ name.UpperCamelCase }};

    function singleStruct(bytes32[{{ slotsLength }}] _{{ name.lowerCamelCase }}Data) public returns(
        {{#each variables}}
        {{ type }} {{ name.lowerCamelCase }}{{#unless @last}},{{/unless}}
        {{/each}}
    ) {
        {{ name.Plural }}.{{ name.UpperCamelCase }} storage _{{ name.lowerCamelCase }};
        _{{ name.lowerCamelCase }}.data = _{{ name.lowerCamelCase }}Data;
        return _{{ name.lowerCamelCase }}.get{{ name.UpperCamelCase }}();
    }

    function knownLengthStructArray(bytes32[{{ slotsLength }}][10] _{{ name.lowerCamelCase }}Data) public returns(
        {{#each variables}}
        {{ type }} {{ name.lowerCamelCase }}{{#unless @last}},{{/unless}}
        {{/each}}
    ) {
        {{ name.Plural }}.{{ name.UpperCamelCase }}[10] storage _{{ name.plural }};
        for (uint i = 0; i < _{{ name.lowerCamelCase }}Data.length; i++) {
            _{{ name.plural }}[i].data = _{{ name.lowerCamelCase }}Data[i];
        }
        return _{{ name.plural }}[0].get{{ name.UpperCamelCase }}();
    }

    function unknownLengthStructArray(
        {{#each slots}}
        bytes32[] _{{ ../name.lowerCamelCase }}DataSlot{{ @index }}{{#unless @last}},{{/unless}}
        {{/each}}
    ) public returns(
        {{#each variables}}
        {{ type }} {{ name.lowerCamelCase }}{{#unless @last}},{{/unless}}
        {{/each}}
    ) {
        {{ name.Plural }}.{{ name.UpperCamelCase }}[] storage _{{ name.plural }};
        _{{ name.plural }}.length = _{{ name.lowerCamelCase }}DataSlot0.length;
        for (uint i = 0; i < _{{ name.lowerCamelCase }}DataSlot0.length; i++) {
            {{#each slots}}
            _{{ ../name.plural }}[i].data[{{ @index }}] = _{{ ../name.lowerCamelCase }}DataSlot{{ @index }}[i];
            {{/each}}
        }
        return _{{ name.plural }}[0].get{{ name.UpperCamelCase }}();
    }
}
